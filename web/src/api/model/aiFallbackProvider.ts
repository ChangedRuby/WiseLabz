/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * WiseLabz REST API
 * Contract-first OpenAPI spec for the WiseLabz frontend. This is the source of truth the frontend generates against (orval) and mocks against (MSW) BEFORE the Go backend exists. The backend implements *to* this spec. Keep this file and docs/WS_CONTRACT.md in sync with docs/FRONTEND_PLAN.md (§1 endpoints, §4 WS).
 *
 * OpenAPI spec version: 0.1.0
 */
import type { AiFallbackProviderProvider } from './aiFallbackProviderProvider';

/**
 * One entry in the ordered fallback chain (priority 2+, since the primary AiConfig.provider is implicitly priority 1). Tried in list order when the provider before it errors with 429, 5xx, or a timeout.
 */
export interface AiFallbackProvider {
  provider: AiFallbackProviderProvider;
  model?: string;
  /** Write-only; omitted in responses */
  apiKey?: string;
  /** For Ollama / self-hosted */
  baseUrl?: string;
}

/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * WiseLabz REST API
 * Contract-first OpenAPI spec for the WiseLabz frontend. This is the source of truth the frontend generates against (orval) and mocks against (MSW) BEFORE the Go backend exists. The backend implements *to* this spec. Keep this file and docs/WS_CONTRACT.md in sync with docs/FRONTEND_PLAN.md (§1 endpoints, §4 WS).
 *
 * OpenAPI spec version: 0.1.0
 */
import type { ReportFindingSummary } from './reportFindingSummary';

export interface ReportComplianceSection {
  error?: string;
  /** Findings with check_type=compliance still open as of report generation. */
  openCount: number;
  /** check_type=compliance findings first detected during the window. */
  detectedCount: number;
  /** check_type=compliance findings resolved during the window. */
  resolvedCount: number;
  items: ReportFindingSummary[];
}
